import 'package:flutter/material.dart';

class DecodedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Decoded View'),
      ),
      body: Center(
        child: Text('Decoded View - Coming Soon'),
      ),
    );
  }
} 